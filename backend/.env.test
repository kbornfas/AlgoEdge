# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Test Database Configuration
DATABASE_URL=postgresql://testuser:testpass@localhost:5432/algoedge_test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing
JWT_EXPIRES_IN=7d

# Email Configuration (disabled for testing)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@example.com
SMTP_PASS=testpass
SMTP_FROM=AlgoEdge Test <noreply@algoedge.test>
EMAIL_BATCH_DELAY_MS=100

# Stripe Configuration (test mode)
STRIPE_SECRET_KEY=sk_test_fake
STRIPE_WEBHOOK_SECRET=whsec_fake
STRIPE_PRICE_ID_PRO=price_fake_pro
STRIPE_PRICE_ID_ENTERPRISE=price_fake_enterprise

# Rate Limiting (relaxed for testing)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Frontend URL
FRONTEND_URL=http://localhost:3001
