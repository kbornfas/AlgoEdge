# Test environment with missing variables
DATABASE_URL="postgresql://user:password@localhost:5432/algoedge"
# JWT_SECRET is missing - should trigger validation error
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
