# Render Blueprint for AlgoEdge Backend
# 
# ⚠️ CRITICAL ARCHITECTURE:
# - Backend deploys to RENDER (this file)
# - Frontend deploys to VERCEL (vercel.json)
# - Database migrations run ONLY here on Render
# 
# This file defines the backend deployment on Render
# 
# Architecture:
# - Frontend: Deployed on Vercel (Next.js only, NO migrations)
# - Backend: Deployed on Render (Express API + Database migrations)
# - Database: PostgreSQL on Render
#
# The backend service is responsible for:
# - Running Prisma database migrations (ONLY place migrations run)
# - Providing REST API endpoints (Express)
# - Handling WebSocket connections for real-time trading
# - Managing all database schema changes

services:
  # Backend API Service (also runs database migrations)
  - type: web
    name: algoedge-backend
    runtime: node
    region: oregon
    plan: starter
    # Build command: Install all dependencies (root + backend) and run migrations
    # Note: Using set -e to fail fast on any error
    buildCommand: |
      set -e
      set -o pipefail
      
      echo ""
      echo "╔════════════════════════════════════════════════════════════════╗"
      echo "║                                                                ║"
      echo "║              AlgoEdge Backend - Render Build                   ║"
      echo "║                                                                ║"
      echo "╚════════════════════════════════════════════════════════════════╝"
      echo ""
      
      # Step 1: Install dependencies
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "  STEP 1/5: Installing Dependencies"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "📦 Installing root dependencies..."
      npm ci --prefix . || { echo ""; echo "❌ Root dependency installation failed"; exit 1; }
      echo "✅ Root dependencies installed"
      echo ""
      
      echo "📦 Installing backend dependencies..."
      npm ci --prefix backend || { echo ""; echo "❌ Backend dependency installation failed"; exit 1; }
      echo "✅ Backend dependencies installed"
      echo ""
      
      # Step 2: Generate Prisma Client
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "  STEP 2/5: Generating Prisma Client"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      npx prisma generate || { echo ""; echo "❌ Prisma generate failed"; echo "Check that prisma/schema.prisma is valid"; exit 1; }
      echo "✅ Prisma Client generated successfully"
      echo ""
      
      # Step 3: Deploy migrations
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "  STEP 3/5: Deploying Database Migrations"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "Running: npx prisma migrate deploy"
      npx prisma migrate deploy || { 
        echo ""
        echo "❌ Migration deployment failed"
        echo ""
        echo "Possible causes:"
        echo "  - DATABASE_URL not configured correctly"
        echo "  - Database not accessible"
        echo "  - Migration files are corrupted"
        echo ""
        echo "Check Render dashboard for DATABASE_URL configuration"
        exit 1
      }
      echo "✅ Migrations deployed successfully"
      echo ""
      
      # Step 4: Verify database setup
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "  STEP 4/5: Verifying Database Schema"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      node scripts/render-validate.js || { 
        echo ""
        echo "❌ Database validation failed"
        echo ""
        echo "The database connection works but the schema is incomplete."
        echo "Check the validation output above for details."
        exit 1
      }
      echo ""
      
      # Step 5: Final verification
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo "  STEP 5/5: Final Build Verification"
      echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      echo ""
      echo "✅ Dependencies installed"
      echo "✅ Prisma Client generated"
      echo "✅ Database migrations deployed"
      echo "✅ Database schema validated"
      echo ""
      echo "╔════════════════════════════════════════════════════════════════╗"
      echo "║                                                                ║"
      echo "║              ✅ Build Completed Successfully                    ║"
      echo "║                                                                ║"
      echo "╚════════════════════════════════════════════════════════════════╝"
      echo ""
      echo "Next: Backend server will start with 'npm start'"
      echo ""
    # Start the Express backend
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: algoedge-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        value: "AlgoEdge <noreply@algoedge.com>"
      - key: FRONTEND_URL
        sync: false
      - key: METAAPI_TOKEN
        sync: false
      - key: METAAPI_ACCOUNT_ID
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
    healthCheckPath: /health
    autoDeploy: true

databases:
  # PostgreSQL Database
  - name: algoedge-db
    plan: starter
    databaseName: algoedge
    user: algoedge
    region: oregon

